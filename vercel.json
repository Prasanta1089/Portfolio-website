{
    "builds":[{
        "src": "project/wsgi.py",
        "use":"@vercel/python",
        "config": {"maxLambadaSize": "15mb", "runtime": "Python3.10.4"}
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"project/wsgi.py"
        }
    ]
}